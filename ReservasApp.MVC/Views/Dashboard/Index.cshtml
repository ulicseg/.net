@model IEnumerable<Reserva>
@{
    ViewData["Title"] = "Panel Principal";
    var usuario = ViewBag.Usuario as Usuario;
    var totalReservas = (int)ViewBag.TotalReservas;
    var proximasReservas = (int)ViewBag.ProximasReservas;
    var reservasCompletadas = (int)ViewBag.ReservasCompletadas;
    var proximaReserva = ViewBag.ProximaReserva as Reserva;
}

<div class="dashboard-container">
    <!-- Bienvenida -->
    <div class="welcome-section">
        <h1>¡Bienvenido/a, @usuario?.Nombre! 👋</h1>
        <p>Tu panel médico personal</p>
    </div>

    <!-- Estadísticas -->
    <div class="stats-grid">
        <div class="stat-card total">
            <div class="stat-icon">📊</div>
            <div class="stat-content">
                <h3>@totalReservas</h3>
                <p>Total Reservas</p>
            </div>
        </div>

        <div class="stat-card proximas">
            <div class="stat-icon">📅</div>
            <div class="stat-content">
                <h3>@proximasReservas</h3>
                <p>Próximas Citas</p>
            </div>
        </div>

        <div class="stat-card completadas">
            <div class="stat-icon">✅</div>
            <div class="stat-content">
                <h3>@reservasCompletadas</h3>
                <p>Completadas</p>
            </div>
        </div>
    </div>

    <!-- Próxima Reserva Destacada -->
    @if (proximaReserva != null)
    {
        <div class="next-appointment">
            <h2>🕐 Próxima Cita</h2>
            <div class="appointment-card">
                <div class="appointment-info">
                    <h3>@proximaReserva.Titulo</h3>
                    <p class="date">📅 @proximaReserva.FechaReserva.ToString("dddd, dd MMMM yyyy")</p>
                    <p class="time">⏰ @proximaReserva.FechaReserva.ToString("HH:mm")</p>
                    <p class="description">@proximaReserva.Descripcion</p>
                </div>
                <div class="appointment-actions">
                    <a asp-controller="Reservas" asp-action="Details" asp-route-id="@proximaReserva.Id" class="btn-details">Ver Detalles</a>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="no-appointments">
            <h2>📋 No tienes citas programadas</h2>
            <p>¡Es un buen momento para programar tu próxima consulta!</p>
            <a asp-controller="Reservas" asp-action="Create" class="btn-create">Nueva Reserva</a>
        </div>
    }

    <!-- Acciones Rápidas -->
    <div class="quick-actions">
        <h2>🚀 Acciones Rápidas</h2>
        <div class="actions-grid">
            <a asp-controller="Reservas" asp-action="Create" class="action-card">
                <div class="action-icon">➕</div>
                <h3>Nueva Reserva</h3>
                <p>Programa una nueva cita médica</p>
            </a>

            <a asp-controller="Reservas" asp-action="Index" class="action-card">
                <div class="action-icon">📋</div>
                <h3>Mis Reservas</h3>
                <p>Ver todas mis citas</p>
            </a>

            <a href="#" class="action-card">
                <div class="action-icon">👤</div>
                <h3>Mi Perfil</h3>
                <p>Actualizar datos personales</p>
            </a>
        </div>
    </div>

    <!-- Lista Rápida de Reservas -->
    @if (Model.Any())
    {
        <div class="recent-reservations">
            <h2>📋 Reservas Recientes</h2>
            <div class="reservations-list">
                @foreach (var reserva in Model.Take(5))
                {
                    <div class="reservation-item @(reserva.FechaReserva > DateTime.Now ? "upcoming" : "completed")">
                        <div class="reservation-info">
                            <h4>@reserva.Titulo</h4>
                            <p class="date">@reserva.FechaReserva.ToString("dd/MM/yyyy HH:mm")</p>
                        </div>
                        <div class="reservation-status">
                            @if (reserva.FechaReserva > DateTime.Now)
                            {
                                <span class="status upcoming">Próxima</span>
                            }
                            else
                            {
                                <span class="status completed">Completada</span>
                            }
                        </div>
                    </div>
                }
            </div>
            
            @if (Model.Count() > 5)
            {
                <div class="view-all">
                    <a asp-controller="Reservas" asp-action="Index">Ver todas las reservas</a>
                </div>
            }
        </div>
    }
</div>

<style>
.dashboard-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px;
    background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
    min-height: 100vh;
}

.welcome-section {
    text-align: center;
    margin-bottom: 30px;
    padding: 20px;
    background: white;
    border-radius: 15px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
}

.welcome-section h1 {
    color: #333;
    margin-bottom: 10px;
    font-size: 2.5rem;
}

.welcome-section p {
    color: #666;
    font-size: 1.1rem;
}

.stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 20px;
    margin-bottom: 30px;
}

.stat-card {
    background: white;
    padding: 25px;
    border-radius: 15px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    display: flex;
    align-items: center;
    transition: transform 0.3s ease;
}

.stat-card:hover {
    transform: translateY(-5px);
}

.stat-card.total { border-left: 5px solid #3498db; }
.stat-card.proximas { border-left: 5px solid #e74c3c; }
.stat-card.completadas { border-left: 5px solid #27ae60; }

.stat-icon {
    font-size: 2.5rem;
    margin-right: 20px;
}

.stat-content h3 {
    font-size: 2rem;
    color: #333;
    margin: 0;
}

.stat-content p {
    color: #666;
    margin: 5px 0 0 0;
}

.next-appointment, .no-appointments {
    background: white;
    padding: 25px;
    border-radius: 15px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    margin-bottom: 30px;
}

.next-appointment h2, .no-appointments h2 {
    color: #333;
    margin-bottom: 20px;
}

.appointment-card {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: #f8f9fa;
    padding: 20px;
    border-radius: 10px;
    border-left: 4px solid #e74c3c;
}

.appointment-info h3 {
    color: #333;
    margin-bottom: 10px;
}

.appointment-info p {
    margin: 5px 0;
    color: #666;
}

.btn-details, .btn-create {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 10px 20px;
    border-radius: 8px;
    text-decoration: none;
    transition: all 0.3s ease;
}

.btn-details:hover, .btn-create:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(102, 126, 234, 0.3);
    color: white;
    text-decoration: none;
}

.no-appointments {
    text-align: center;
}

.no-appointments p {
    color: #666;
    margin-bottom: 20px;
}

.quick-actions {
    background: white;
    padding: 25px;
    border-radius: 15px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    margin-bottom: 30px;
}

.quick-actions h2 {
    color: #333;
    margin-bottom: 20px;
}

.actions-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 20px;
}

.action-card {
    background: #f8f9fa;
    padding: 20px;
    border-radius: 10px;
    text-decoration: none;
    color: #333;
    transition: all 0.3s ease;
    text-align: center;
}

.action-card:hover {
    background: #e9ecef;
    transform: translateY(-3px);
    color: #333;
    text-decoration: none;
}

.action-icon {
    font-size: 2rem;
    margin-bottom: 10px;
}

.action-card h3 {
    margin: 10px 0;
    color: #333;
}

.action-card p {
    color: #666;
    font-size: 0.9rem;
}

.recent-reservations {
    background: white;
    padding: 25px;
    border-radius: 15px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
}

.recent-reservations h2 {
    color: #333;
    margin-bottom: 20px;
}

.reservations-list {
    space-y: 10px;
}

.reservation-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px;
    background: #f8f9fa;
    border-radius: 8px;
    margin-bottom: 10px;
}

.reservation-item.upcoming {
    border-left: 4px solid #e74c3c;
}

.reservation-item.completed {
    border-left: 4px solid #27ae60;
}

.reservation-info h4 {
    margin: 0;
    color: #333;
}

.reservation-info .date {
    color: #666;
    font-size: 0.9rem;
    margin: 5px 0 0 0;
}

.status {
    padding: 5px 12px;
    border-radius: 20px;
    font-size: 0.8rem;
    font-weight: 600;
}

.status.upcoming {
    background: #fff5f5;
    color: #e74c3c;
}

.status.completed {
    background: #f0fff4;
    color: #27ae60;
}

.view-all {
    text-align: center;
    margin-top: 20px;
}

.view-all a {
    color: #667eea;
    text-decoration: none;
    font-weight: 600;
}

.view-all a:hover {
    text-decoration: underline;
}

@@media (max-width: 768px) {
    .dashboard-container {
        padding: 10px;
    }
    
    .stats-grid {
        grid-template-columns: 1fr;
    }
    
    .appointment-card {
        flex-direction: column;
        text-align: center;
    }
    
    .appointment-actions {
        margin-top: 15px;
    }
    
    .actions-grid {
        grid-template-columns: 1fr;
    }
}
</style> 